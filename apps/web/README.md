# CTX Web

Next.js App Router dashboard and API for CTX.

## Run

```bash
pnpm --filter @ctx/web dev
```

Use the root command for the full local setup:

```bash
pnpm dev
```

The web app owns the Prisma SQLite schema, capsule CRUD API, GitHub capture API, search, stats, activity logs, and the dashboard UI.
