# Contributing

Use the root `CONTRIBUTING.md` for contribution policy. This document captures CTX-specific product expectations:

- Preserve local-first behavior.
- Validate external inputs with Zod.
- Keep browser extension selectors conservative.
- Do not pretend an integration succeeded when credentials are missing.
- Add tests for shared package behavior.
